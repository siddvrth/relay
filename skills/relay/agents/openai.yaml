interface:
  display_name: "Relay"
  short_description: "Automatic fresh-thread Goal continuity"
  default_prompt: "Relay attempts a verified fresh Codex Goal continuation at automatic compaction and fails open to native compaction when it cannot hand off safely."
